#include "pch.h"
#include "test.h"

void test(const IndexData &index, const DataSet &data)
{
	//TopLimitFactor testTLF;
	//testTLF.init(0.1, index, data);
	//
	//long long date = 20180320;

	//vector<long long> idxcon;
	//index.getPool()->getSnapshot(date, &idxcon);
	//size_t size = idxcon.size();

	//vector<double> mv(size);

	//double totalmv = 0;
	//auto iter_mv = mv.begin();
	//for (auto iter = idxcon.begin(); iter != idxcon.end(); ++iter, ++iter_mv) {
	//	*iter_mv = data.close(*iter, date) * data.freeshare(*iter, date);
	//	totalmv += *iter_mv;
	//}

	//vector<double> factor;
	//testTLF.getFactor(date, idxcon, &factor);

	//iter_mv = mv.begin();
	//auto iter_factor = factor.begin();
	//for (auto iter = idxcon.begin(); iter != idxcon.end(); ++iter, ++iter_mv, ++iter_factor) {
	//	std::cout << *iter << ',' << *iter_mv << ',' << *iter_factor << std::endl;
	//}
	//std::cout << fromExcelDate(43544) << std::endl;
}
